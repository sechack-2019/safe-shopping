version: 2
jobs:
  build:
    docker:
      - image: circleci/node:latest
    steps:
      - checkout
      - run:
          name: Greeting
          command: echo Hello, world.
      - run:
          name: install node paclage
          command: npm install chrome/
      - run:
          name: Print the Current Time
          command: date
